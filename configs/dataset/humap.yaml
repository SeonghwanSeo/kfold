# Synthetic dataset configuration from huMAP.
_yaml_: "default-train.yaml"

name: humap
weight: 0.04

# Data paths
data_path: <DATA_PATH_PLACEHOLDER>
seed: null # should be null for data augmentation

sampler: null

cropper:
  _registry_: "data_cropper"
  _class_: AlphaFold3Cropper
  w_contiguous: 0.0
  w_spatial: 0.0
  w_spatial_interface: 1.0

apo_init:
  # no ligand
  ligand_perturbation: null
