# Default dataset configuration for training
_yaml_: "default.yaml"

name: ???
weight: 1.0 # sample weight for this dataset

# Data paths
data_path: <DATA_PATH_PLACEHOLDER>
seed: null # should be null for data augmentation

sampler: ???
cropper: ???

apo_init:
  # Enable perturbation during training
  prob_perturbation: 0.95
  use_residue_permutation: true
  use_holo_if_apo_unavailable: true
  use_cached_conformer_only: true
  protein_perturbation:
    prob_rieprody: 0.0 # Default: use metric-free BioPrior perturbation
    rieprody: null
    bioprior:
      min_steps: 1 # Minimum random walk steps
      max_steps: 15 # Maximum random walk steps
      log_level: "INFO" # Logging level
  ligand_perturbation:
    mode: "gaussian" # Perturbation mode: "gaussian"
    gaussian_sigma: 0.3
    langevin_num_steps: 200
    langevin_dt: 0.005
    langevin_k_bond: 50.0
    langevin_k_angle: 10.0
    langevin_temperature: 1.0
    recenter: true
